language: cpp
compiler:
  - clang
  - gcc
os:
  - linux
before_install:
  - sudo apt-get -y install ninja-build
script:
  - cmake -G "Ninja Multi-Config" -B build; ninja -j0 -C build
branches:
  only:
    - main
